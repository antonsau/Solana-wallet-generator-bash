# Solana wallet generator (bash)

Bash script for generating Solana wallets.

Copy script.sh to the folder where you want to save txt with wallets and run the command "bash script.sh" in the terminal.
